# Copy to .env on each machine — .env is gitignored and never leaves the laptop.
#   cd <repo root> && echo "GH_TOKEN=$(gh auth token)" > test_api/.env
#
# Needs `gh auth login` first. The `repo` scope covers everything in curator.http.
GH_TOKEN=ghp_replace_me
